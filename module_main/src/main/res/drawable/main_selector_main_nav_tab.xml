<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true"
        android:color="@color/color_primary"/>          <!-- selected颜色 -->
    <item android:state_pressed="true"
        android:color="@color/color_primary"/>          <!-- pressed颜色 -->
    <item android:state_focused="true"
        android:color="@color/color_primary"/>          <!-- focused颜色 -->
    <item android:color="@color/color_gray_999999"/>    <!-- default颜色 -->
</selector>